# ── Node.js ──────────────────────────────────────────
node_modules/

# ── Environment & Secrets (NEVER commit) ─────────────
.env
.env.production
.env.local

# ── WhatsApp Session Data ────────────────────────────
.wwebjs_auth/
.wwebjs_cache/
storage/

# ── Logs ─────────────────────────────────────────────
logs/*.log
temp/

# ── Test Artifacts ────────────────────────────────────
test_output.txt
*.log

# ── OS Files ─────────────────────────────────────────
.DS_Store
Thumbs.db

# ── SQLite (legacy — replaced by PostgreSQL) ─────────
database.db

# ── React Landing Page Build Output ──────────────────
taqreerk/dist/
taqreerk/node_modules/

# ── Large test files in project root ─────────────────
grid_test.png
template.png

# ── Attached assets (prompt dumps, auto-generated images) ──
attached_assets/*.txt
